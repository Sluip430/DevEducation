describe('cyclesHomeWork', () => {
 
    describe('On sumNum', () => {

            it('should sum of array from 1-99 and count', () => {
                expect(sumNum(1, 10)).to.be.an('array').that.includes(5);;
            });
        })   
        
    
});
